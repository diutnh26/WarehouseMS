<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox spacing="16" xmlns:fx="http://javafx.com/fxml" fx:controller="com.warehouse.controller.StockExportController">
    <padding><Insets top="20" right="24" bottom="20" left="24"/></padding>

    <!-- Page Header -->
    <VBox spacing="2">
        <Label text="Stock Export" styleClass="page-title"/>
        <Label text="Home / Transactions / Stock Export" styleClass="breadcrumb-label"/>
    </VBox>

    <!-- Split View -->
    <HBox spacing="16" VBox.vgrow="ALWAYS">

        <!-- Left: Export History -->
        <VBox styleClass="recent-card" HBox.hgrow="ALWAYS">
            <Label text="Export History" styleClass="card-title"
                   style="-fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 16 20;"/>
            <TableView fx:id="tblExports" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colExportId" text="ID" prefWidth="60"/>
                    <TableColumn fx:id="colDate" text="Date" prefWidth="120"/>
                    <TableColumn fx:id="colReason" text="Reason" prefWidth="150"/>
                    <TableColumn fx:id="colCreatedBy" text="Created By" prefWidth="140"/>
                </columns>
            </TableView>
        </VBox>

        <!-- Right: New Export Form -->
        <VBox spacing="0" styleClass="recent-card" prefWidth="500" minWidth="450">
            <Label text="New Export" styleClass="card-title"
                   style="-fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 16 20;"/>

            <VBox spacing="14" style="-fx-padding: 0 20 10 20;">
                <!-- Row: Date + Reason -->
                <HBox spacing="16">
                    <VBox spacing="6" prefWidth="180">
                        <Label text="EXPORT DATE" styleClass="form-label"/>
                        <DatePicker fx:id="dpExportDate" maxWidth="Infinity"/>
                    </VBox>
                    <VBox spacing="6" HBox.hgrow="ALWAYS">
                        <Label text="REASON" styleClass="form-label"/>
                        <TextField fx:id="txtReason" promptText="e.g. Customer sale, Internal use"/>
                    </VBox>
                </HBox>

                <!-- Notes -->
                <VBox spacing="6">
                    <Label text="NOTES" styleClass="form-label"/>
                    <TextArea fx:id="txtNotes" promptText="Optional notes..." prefRowCount="1" maxHeight="36"/>
                </VBox>
            </VBox>

            <!-- Add Product Line -->
            <VBox spacing="8" style="-fx-padding: 10 20; -fx-background-color: #F1F5F9;">
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <VBox spacing="4" HBox.hgrow="ALWAYS">
                        <Label text="PRODUCT" styleClass="form-label" style="-fx-font-size: 10px;"/>
                        <ComboBox fx:id="cmbProduct" maxWidth="Infinity"/>
                    </VBox>
                    <VBox spacing="4" prefWidth="100">
                        <Label text="QTY" styleClass="form-label" style="-fx-font-size: 10px;"/>
                        <TextField fx:id="txtQuantity"/>
                    </VBox>
                </HBox>
                <HBox spacing="10" alignment="CENTER_RIGHT">
                    <Button text="+ Add" onAction="#handleAddLine"
                            style="-fx-background-color: #10B981; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 8 20; -fx-background-radius: 6; -fx-cursor: hand;"/>
                    <Button text="- Remove" onAction="#handleRemoveLine"
                            style="-fx-background-color: #EF4444; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 8 20; -fx-background-radius: 6; -fx-cursor: hand;"/>
                </HBox>
            </VBox>

            <!-- Detail Table -->
            <TableView fx:id="tblDetails" VBox.vgrow="ALWAYS" prefHeight="200">
                <columns>
                    <TableColumn fx:id="colDetailProduct" text="Product" prefWidth="280"/>
                    <TableColumn fx:id="colDetailQty" text="Quantity" prefWidth="100"/>
                </columns>
            </TableView>

            <!-- Save / Clear -->
            <HBox spacing="10" style="-fx-padding: 12 20;">
                <Button text="Save Export" onAction="#handleSaveExport" styleClass="btn-primary" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
                <Button text="Clear" onAction="#handleClear" styleClass="btn-secondary"/>
            </HBox>
        </VBox>
    </HBox>
</VBox>